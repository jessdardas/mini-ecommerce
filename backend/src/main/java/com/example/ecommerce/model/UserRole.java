package com.example.ecommerce.model;

// This enum defines the roles a user can have
public enum UserRole {
    USER,   // Regular customer
    ADMIN   // Admin who can add products, view all orders, etc.
}
